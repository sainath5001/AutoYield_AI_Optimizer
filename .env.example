# Root convenience: copy values into `frontend/.env.local` and `backend/.env`
# as needed. Do not commit real secrets.

# --- Frontend (also: frontend/.env.local) ---
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# --- Backend (also: backend/.env) ---
PORT=4000
LIFI_API_KEY=
OPENAI_API_KEY=
